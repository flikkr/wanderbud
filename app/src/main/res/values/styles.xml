<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSunset</item>

        <item name="android:textViewStyle">@font/montserrat_regular</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--  Button styles  -->
    <style name="Base.TextAppearance.AppCompat.Button">
        <item name="textAllCaps">false</item>
        <item name="android:background">@drawable/button</item>
        <item name="font">@font/montserrat_regular</item>
        <item name="android:colorPressedHighlight">@color/colorPrimaryAccent</item>
    </style>

</resources>
